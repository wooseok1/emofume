import 'package:flutter/material.dart';

class MyPerfume extends StatelessWidget {
  const MyPerfume({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('myperfume'),),
    );
  }
}

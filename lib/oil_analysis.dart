import 'package:flutter/material.dart';

class OilAnalysis extends StatelessWidget {
  const OilAnalysis({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('oilanalysis'),),
    );
  }
}
